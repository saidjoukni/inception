# 🏗️ Inception Project: ملخص شامل للمشروع



### ١. المفهوم الأساسي (Docker vs VM)

* **المكينة الافتراضية `Virtual Machine`**: هي PC افتراضي كامل بنظام تشغيل ثقيل كتحطو فوق الـ PC ديالك.

* **الحاوية `Docker Container`**: هو "صندوق" خفيف ومعزول كيتشارك مع الـ VM نفس النواة `Kernel`.

* **الفائدة**: العزل التام للخدمات وسهولة نقلها وتشغيلها في أي مكان بنفس الطريقة.



---



### ٢. بنية التحتية (Infrastructure Layout)

المشروع كيتكون من ٣ صناديق أساسية خاصك تصاوبهم بيدك `Dockerfiles`:

* **خدمة `NGINX`**: هو "الباب" الوحيد للموقع، كيدخل الناس عبر `Port 443` وكيخدم بتشفير `TLSv1.2` أو `TLSv1.3` فقط.

* **خدمة `WordPress + php-fpm`**: هو "المحرك" اللي فيه الكود ديال الموقع، كيهضر مع NGINX ومع قاعدة البيانات.

* **خدمة `MariaDB`**: هو "المخزن" ديال البيانات (المقالات، المستخدمين)، كيكون معزول لداخل بلا Nginx.



---



### ٣. التواصل والتخزين (Networking & Volumes)

* **الشبكة `Docker Network`**: هو "الخيط" الافتراضي اللي كيربط الصناديق ببعضياتهم باش يتواصلوا بالاسم `DNS` بلا ما يحتاجوا `IP`.

* **الخزنات `Docker Volumes`**: هما اللي كتحافظ على البيانات للأبد وخا تمسح الصندوق.

    * السوجي كيفرض تخزين البيانات في المسار: `/home/login/data/`.



---



### ٤. القواعد الذهبية (الخطوط الحمراء)

* **قاعدة `PID 1`**: الخدمة الأساسية هي اللي خاصها تكون شادة الـ Container، ممنوع أوامر الترقيع بحال `tail -f` أو `sleep infinity`.

* **الأمن `Secrets`**: ممنوع نهائياً تحط كلمات السر فـ الكود أو `Dockerfile`. خاص تستعمل ملفات `.env` و `Docker Secrets`.

* **التحكم**: ممنوع استعمل صور واجدة `Ready-made images`؛ خاصك تصاوب كلشي بيدك من الصفر.



---



### ٥. 🚀 كيفاش كايخدم `Docker` و `Docker Compose` ؟

بكل بساطة، `Docker` كايتكلف بصناعة "الصناديق" المعزولة، و `Docker Compose` كايتكلف بتسيير "الفرقة" كاملة.

#### ■ دور `Docker` (صناعة الحاوية):

* **الـ `Dockerfile`**: هو الورقة اللي فيها الأوامر (الوصفة) باش نصاوبو الخدمة.

* **الـ `Image`**: هي "النسخة الأصلية" (المجمدة) اللي فيها السيستيم والبرامج واجدين.

* **الـ `Container`**: هو فاش كانديرو `Run` لليماج وكتولي عملية (`Process`) خدامة ومعزولة وسط الـ `VM`.

#### ■ دور `Docker Compose` (تنسيق المشروع):

* **التجميع**: كايجمع خدمات بزاف (`NGINX`، `WordPress`، `MariaDB`) فملف واحد سميتو `docker-compose.yml`.

* **الشبكة (`Network`)**: كايصاوب "خيط" افتراضي كايخلي الصناديق يهضرو بيناتهم بالسمية ديالهم.

* **التخزين (`Volumes`)**: كايعرف "الخزنات" اللي كاتحفظ لينا البيانات (مثل المقالات) باش ما تضيعش وخا يتمسح الصندوق.

* **الأتمتة**: بـ أمر واحد كايقدر يبني الصور (`Images`) ويطلق كاع الصناديق بالترتيب الصحيح.

#### 💡 خلاصة للمقارنة:

* **`Docker`**: كايصاوب ليك "صندوق" (`Container`) واحد من "وصفة" (`Dockerfile`).

* **`Docker Compose`**: هو "المايسترو" اللي كايطلق بزاف د الصناديق فدقة وحدة وكايخليهم يتواصلو بيناتهم بطريقة منظمة.
# 🛠️ Docker & WP-CLI Commands Cheatsheet (Inception)

## 1. إدارة المشروع (Docker Compose)
هادو هما الكوماندات باش كتحكم في المشروع كامل (ديما تأكد أنك وسط المجلد اللي فيه `docker-compose.yml`):

* **`docker compose up --build -d`**
  * **الدور:** كيبني الحاويات من جديد (`--build`)، كيشعلهم كاملين (`up`)، وكيخليهم خدامين في الكواليس (`-d`) باش يبقى التيرمينال ديالك مطلوق.
  * **وقتاش نستعملها:** ملي كنبغي نلونصي المشروع، أو ملي كندير شي تغيير في `Dockerfile` أو `.env` وبغيت نطبقو.

* **`docker compose down -v`**
  * **الدور:** كيطفي الحاويات (`down`)، وكيمسحهم، والأهم أنه كيمسح حتى الـ Volumes (`-v`) يعني كيمسح قاعدة البيانات وملفات الموقع.
  * **وقتاش نستعملها:** ملي كنبغي نبدا التيست من الزيرو على نظافة باش السكريبت يعاود يخدم من الأول. *(⚠️ تحذير: هادي خطيرة وممنوع تدار في مشاريع حقيقية خدامة باش ماطيرش الداتا).*

---

## 2. المراقبة والتيست (Inspection & Debugging)
هادو باش كتعرف شنو واقع لداخل يلا طاح ليك شي Container:

* **`docker ps`**
  * **الدور:** كيعطيك اللائحة ديال الحاويات اللي شاعلين دابا (`Up`)، أو اللي كيكراشيو ويديرو (`Restarting`).

* **`docker logs <container_name>`**
  * **مثال:** `docker logs wordpress` أو `docker logs mariadb`
  * **الدور:** كيقرا ليك السجل ديال الأخطاء (Logs). هادا هو أهم سلاح عندك باش تعرف السكريبت فين وقف وعلاش الحاوية مابغاتش تشعل.

* **`docker exec -it <container_name> bash`**
  * **مثال:** `docker exec -it wordpress bash`
  * **الدور:** كيحل ليك تيرمينال لداخل ديال الحاوية باش تدخل دير جولة تفتيشية وتشوف الملفات بعينيك. *(للخروج، كتب `exit`).*

---

## 3. أوامر WordPress (WP-CLI Commands)
هادو كتخدم بيهم **فقط** ملي كتكون لداخل ديال الحاوية ديال ووردبريس (وخاصك ديما تكون واقف في المجلد `/var/www/html` وتزيد `--allow-root`):

* **`wp db check --allow-root`**
  * **الدور:** كيتأكد واش ووردبريس مكونيكطي بنجاح مع قاعدة البيانات (MariaDB).

* **`wp user list --allow-root`**
  * **الدور:** كيعطيك جدول فيه كاع المستخدمين اللي تكرياو (الأدمين واليوزر العادي) والأدوار ديالهم، باش تتأكد بلي السكريبت دار خدمتو صحيحة ومدرتيش كلمة `admin` في السمية.

  # 🧠 خلاصة: علاش كنستعملو `daemon off;` في Docker؟

## 1. شنو هو PID 1؟ (القلب النابض)
* **المعنى:** Process ID 1 هو الرقم اللي كيتعطى لأول وأهم برنامج كيشعل وسط الحاوية (Container).
* **قاعدة Docker:** Docker كيحضى غير هاد PID 1. مادام هاد البرنامج خدام في **الواجهة (Foreground)**، الحاوية كتبقى شاعلة. نهار يحبس ولا يمشي للكواليس، Docker كيسحاب ليه الخدمة سالات وكيطفي الحاوية.

## 2. المشكل ديال NGINX (عزيز عليه يتخبى)
* في العادي، NGINX مبرمج باش يخدم كـ **Daemon**.
* **Daemon = Background (الكواليس):** يعني ملي كيشعل، كيمشي يتخبى اللور.
* **النتيجة الكارثية:** ملي NGINX كيمشي للكواليس، كيخلي البلاصة ديال PID 1 خاوية في الواجهة. Docker كيشوفها خاوية، كيطفي ليك الحاوية في البلاصة (Container Exited).

## 3. الحل السحري: `daemon off;`
* باش نحلّو هاد المشكل، كنزيدو هاد السطر في اللخر ديال Dockerfile:
  `CMD ["nginx", "-g", "daemon off;"]`
* **الدور ديالها:** كتقول لـ NGINX: *"ممنوع تمشي للكواليس (Daemon off)، بقى واقف ديما في الواجهة (Foreground) باش Docker يشوفك ديما خدام ومايطفيش الحاوية"*.

# 🧠 خلاصة إعدادات السرباي NGINX (Cheatsheet)

## 1. الفرق بين URI و URL (للمعاقين 😂)
* **URI (Uniform Resource Identifier):** هو الوصف ديال الحاجة بوحدها (مثلا `/index.php`).
* **URL (Uniform Resource Locator):** هو العنوان الكامل ديال الحاجة (مثلا `https://sjoukni.42.fr/index.php`).
* **القاعدة:** أي URL راه في حد ذاتو URI، ولكن ماشي العكس. في إعدادات السيرفر، ملي كنقولو URI كنقصدو غير داك المسار اللخر.


# 🔐 خلاصة OpenSSL وشهادات الأمان (Cheatsheet)

## 1. شنو هو OpenSSL؟
* **البروتوكولات (TLS/SSL):** هما قوانين ومعايير كتحمي التواصل في الشبكة (كتوفر التشفير، تأكيد الهوية، وسلامة البيانات).
* **الأداة (OpenSSL):** هي البرنامج (Toolkit/CLI) اللي كيطبق هاد القوانين وكيسيرها على أرض الواقع.
* **[Lilmo3a9in] {بـ الخشيبات}:**
  * TLS/SSL هما غير "البلان" (Blueprints/Specifications) في الورقة.
  * OpenSSL هو "البناي" اللي كيطبق داك البلان: هو اللي كيشفر وكيفك التشفير، كيصاوب الشهادات، وكيتكلف بـ "التبزنيسة" (TLS Handshake) بين السيرفر والكليان.

## 2. علاش كنستعملو OpenSSL فهاد المشروع؟
* السابجيكت كيفرض عليك تخدم بـ **TLSv1.2 أو TLSv1.3** مع NGINX. باش يتحقق هادشي، خاصك شهادات أمان (Certificates)، و `OpenSSL` هو الأداة القياسية باش نصاوبوهم.
* **علاش صاوبنا المجلد `/etc/nginx/ssl`؟** حيت NGINX كيحتاج مسار فين يخزن الشهادة والساروت الخاص باش يقراهم ملي يبغي يخدم HTTPS.

## 3. شنو هي شهادة الأمان (SSL Certificate)؟
هي ملف رقمي كيتبت الهوية ديال الموقع وكيسمح ليه يخدم باتصال مشفر (HTTPS).

* 📂 **كتكون من 2 ملفات أساسية:**
  1. **الساروت الخاص (Private Key):** كيبقى مخبي وسري في السيرفر، وكيتستعمل للتشفير وفك التشفير.
  2. **ملف الشهادة (Certificate File):** كيتعطى للمتصفح (البراوزر) باش يتأكد من هوية الموقع ويبدا الاتصال.

* 🔄 **كيفاش كتخدم؟ (السيناريو):**
  1. كتكتب `https://your-site.com`، المتصفح كيطلب من السيرفر الشهادة ديالو.
  2. السيرفر كيرد عليه بملف الشهادة.
  3. المتصفح كيتأكد واش هاد الشهادة موثوقة (موقعة من جهة معترفة CA بحال Let's Encrypt).
  4. إيلا كانت صحيحة، كيبدا الاتصال المشفر بيناتهم.

* 🔐 **شنو هي الشهادة الموقعة ذاتياً (Self-signed SSL)؟**
  هي شهادة صاوبتيها وسنيتيها لراسك (بحال اللي غنديرو حنا)، وماشي مصاوباها شي جهة موثوقة (CA). داكشي علاش المتصفح كيشك فيها في الأول وكيعطيك الشاشة الحمراء ديال التحذير.

---

## 2. بلوك الخادم (Server Block)
كل بلوك `server` كيعتابر سيرفر افتراضي بوحدو، كيجاوب غير على الطلبات اللي كطابق الشروط ديالو.

```nginx
server {
    # 1. الاستقبال والتشفير
    listen 443 ssl;                                  # حل الباب 443 لاتصالات HTTPS المشفرة
    ssl_protocols TLSv1.2 TLSv1.3;                   # فرض التشفير الجديد الآمن ومنع النسخ القديمة
    ssl_certificate /etc/nginx/ssl/inception.crt;    # مسار الشهادة (لاكارط اللي كيشوفها الكليان)
    ssl_certificate_key /etc/nginx/ssl/inception.key;# مسار الساروت السري (باش كيفك التشفير)

    # 2. معلومات الموقع
    server_name sjoukni.42.fr;                       # الدومين اللي كيجاوب عليه هاد السيرفر
    root /var/www/html;                              # 🚨 مسار الملفات (الكوزينة فين كاين ووردبريس)
    index index.php index.html;                      # الملفات الافتراضية اللي كيقلب عليهم بالترتيب

    # 3. معالجة الطلبات العادية (Static Files)
    location / {
        try_files $uri $uri/ /index.php?$args;       # يلا مالقاش الملف، دوز الطلب لـ WordPress باش يخدم الروتات ديالو
    }

    # 4. معالجة ملفات PHP (الربط مع WordPress)
    location ~ \.php$ {
        include snippets/fastcgi-php.conf;           # جيب إعدادات قراءة PHP الافتراضية
        fastcgi_pass wordpress:9000;                 # صيفط الطلب للحاوية ديال wordpress فالبورت 9000
    }
}

```

---

# ✅ ملخص الإصلاحات اللي تزادو باش Mandatory يكون واجد

## 1. `Makefile` ولا خدام
* قبل كان `Makefile` خاوي، يعني evaluator ملي يدير `make` غادي يلقى والو.
* دابا تزادو targets مهمين:
  * `make` / `make up`: كايصاوب `/home/sjoukni/data/mariadb` و `/home/sjoukni/data/wordpress` وكيطلع المشروع.
  * `make down`: كيحبس ويحيد الحاويات.
  * `make fclean`: كيمسح الحاويات، volumes، والداتا باش ترجع تبدا من الزيرو.
  * `make re`: clean rebuild كامل.

## 2. الصور ولاو عندهم نفس سميات الخدمات
* فـ `docker-compose.yml` تزاد:
  * `image: mariadb:inception`
  * `image: wordpress:inception`
  * `image: nginx:inception`
* هادي مهمة حيث السابجيكت كايقول كل image خاصها تكون بنفس سميت service ديالها، وزدنا tag واضح باش مايبقاش Docker يستعمل `latest` بشكل implicit.

## 3. تبدل Base Image لـ `debian:bookworm`
* قبل كان المشروع خدام بـ `debian:bullseye`.
* دابا تبدل لـ `debian:bookworm` حيث هو penultimate stable Debian فهاد الوقت.
* حتى PHP تبدل المسار ديالو من `7.4` لـ `8.2` حيث Bookworm كايجيب PHP 8.2.

## 4. Secrets تفعّلو مزيان
* قبل passwords كانو مكتوبين مباشرة فـ `.env`، وملفات `secrets` خاويين.
* دابا `.env` بقات غير للـ config اللي ماشي sensitive بحال:
  * `DOMAIN_NAME`
  * `MYSQL_DATABASE`
  * usernames و emails
* passwords ولات كتتقرا من:
  * `secrets/db_password.txt`
  * `secrets/db_root_password.txt`
  * `secrets/credentials.txt`
* Docker Compose كايدخلهم للحاويات فـ `/run/secrets/...`.

## 5. MariaDB setup ولى قوي وماكيكملش فوق الغلط
* تزاد `set -eu` باش السكريبت يوقف إلا وقع error.
* تزادت initialization ديال `/var/lib/mysql` إلا كان volume خاوي.
* root password ولى كيتsetta فعلاً.
* database و user ديال WordPress كيتصاوبو بطريقة idempotent.
* فالأخير MariaDB كاتخدم بـ `exec mariadbd` فالواجهة، يعني هي PID 1 وماكاين لا `tail -f` لا `sleep infinity`.

## 6. WordPress setup ولى كيصلح حتى partial install
* قبل كان غير كيشوف واش `wp-config.php` كاين، وهذا يقدر يخلي installation ناقصة.
* دابا كايشيك:
  * واش WordPress core downloaded.
  * واش `wp-config.php` موجود.
  * واش WordPress installed فعلاً.
  * واش اليوزر العادي موجود.
* إلا شي خطوة ناقصة كايكملها، وماكيبقاش volume نصف مركب.

## 7. NGINX routing تصلح لـ WordPress
* قبل:
  `try_files $uri $uri/ =404;`
* هادي كتقتل permalinks ديال WordPress وكتعطي 404 بزاف.
* دابا:
  `try_files $uri $uri/ /index.php?$args;`
* يعني إلا NGINX مالقاش file مباشر، كيدوز الطلب لـ WordPress باش هو يعالج route.

## 8. Docs المطلوبين تزادو
* تزادو فـ root ديال repo:
  * `README.md`
  * `USER_DOC.md`
  * `DEV_DOC.md`
* `README.md` فيه description، instructions، resources، وcomparisons اللي طالبهم السابجيكت:
  * VM vs Docker
  * Secrets vs Environment Variables
  * Docker Network vs Host Network
  * Docker Volumes vs Bind Mounts

## 9. Checklist سريع قبل الدفاع
* `cd Inception && make`
* `docker ps`
* دخل لـ `https://sjoukni.42.fr`
* دخل لـ `https://sjoukni.42.fr/wp-admin`
* جرب:
  `docker exec -it wordpress wp user list --allow-root --path=/var/www/html`
* تأكد أن NGINX هو الوحيد اللي فاتح port `443`.

## 43. تشريح Dockerfile الخاص بـ MariaDB
* `FROM debian:bookworm` : استعمال نسخة Bookworm الحديثة لضمان توافق أفضل وتحديثات أمنية أقوى، التزاماً بقاعدة بناء الصور من الصفر (بدون استعمال FROM mariadb).
* `RUN apt-get update` : دمجنا التحديث، التثبيت، والتنظيف في سطر واحد لتقليل عدد طبقات الصورة (Layers).
* `--no-install-recommends` : تمنع تثبيت الحزم غير الضرورية لتصغير الحجم النهائي للصورة.
* `rm -rf /var/lib/apt/lists/*` : مسح ذاكرة التخزين المؤقت لأداة apt لتخفيف وزن الحاوية.
* `COPY conf/my.cnf ...` : استبدال الإعدادات الافتراضية بملفنا الخاص، والهدف الرئيسي منه هو تغيير bind-address إلى 0.0.0.0 للسماح لـ WordPress بالاتصال بقاعدة البيانات.
* `RUN chmod 755 /setup.sh` : إعطاء صلاحية التنفيذ للسكريبت لتفادي خطأ Permission denied عند إقلاع الحاوية.
* `CMD ["bash", "/setup.sh"]` : هو الأمر الذي يتم تنفيذه عند تشغيل الحاوية (وليس عند بنائها)، وهو المسؤول عن تهيئة قاعدة البيانات وإبقاء العملية الرئيسية تعمل في الواجهة كـ PID 1.

----

## 44. تشريح ملف الإعدادات my.cnf الخاص بـ MariaDB
* `[mysqld]` : هادا عنوان القسم (Section). كيقول لـ MariaDB بلي الإعدادات اللي لتحت خاصين بالسيرفر الرئيسي (الـ Daemon) ماشي بالـ Client.
* `user = mysql` : يحدد المستخدم (System User) الذي سيشغل عملية قاعدة البيانات. من الناحية الأمنية (Security)، يُمنع تشغيل قاعدة البيانات بصلاحيات الـ Root، لذلك نستخدم المستخدم الافتراضي mysql.
* `port = 3306` : تحديد المنفذ (Port) الافتراضي الذي ستستمع فيه MariaDB للطلبات القادمة.
* `datadir = /var/lib/mysql` : تحديد المسار الذي تُخزن فيه قاعدة البيانات ملفاتها والجداول. (وهذا هو نفس المسار الذي نربطه بالـ Volume في docker-compose لضمان بقاء البيانات بعد إغلاق الحاوية).
* `bind-address = 0.0.0.0` : أهم سطر في الملف! افتراضياً، MariaDB تقبل الاتصال فقط من داخل حاويتها (`127.0.0.1`). بتغيير القيمة إلى `0.0.0.0`، نسمح لها باستقبال الاتصالات من أي جهاز آخر على شبكة Docker الداخلية (أي من حاوية WordPress).

----

## 45. أسئلة الدفاع (Defense): إعدادات MariaDB (my.cnf)
* `user = mysql` : تشغيل قاعدة البيانات بمستخدم محدود الصلاحيات بدلاً من root لتفادي السيطرة الكاملة على الحاوية في حالة الاختراق (أمان النظام).
* `port = 3306` : المنفذ الافتراضي والعالمي لقواعد بيانات MySQL/MariaDB، نتركه كما هو لتسهيل اتصال الخدمات الأخرى (مثل WordPress) دون إعدادات إضافية.
* `bind-address = 0.0.0.0` : السماح لقاعدة البيانات باستقبال الاتصالات من جميع الشبكات المتاحة (خصوصاً شبكة Docker الداخلية)، بدلاً من الانغلاق على الحاوية نفسها (127.0.0.1).


----

## 45. System Design: Network Drivers & Docker Routing
* `Driver Concept` : طبقة تجريد (Abstraction Layer) ومترجم بين نظام التشغيل والعتاد (أو الواجهات الوهمية).
* `Bridge Driver` : المشغل الافتراضي لشبكات دوكر. يقوم بإنشاء شبكة وهمية معزولة توفر تواصلاً آمناً بين الحاويات باستخدام تقنية NAT.
* `Host Driver` : يزيل العزل الشبكي تماماً، ويربط الحاوية مباشرة بمنافذ الآلة المضيفة (Host). يوفر أداءً عالياً لكنه يشكل خطراً أمنياً.
* `Custom Bridge vs Default Bridge` : الشبكة المخصصة (Custom Bridge) تُفعّل نظام الـ DNS الداخلي لـ Docker. هذا قرار معماري حاسم يسمح للحاويات باكتشاف والاتصال ببعضها البعض باستخدام الأسماء (مثل `ping mariadb`) عوض الاعتماد على عناوين IP ثابتة (Static IPs).

---
## 48. هندسة التخزين (Volumes) وأسئلة التقييم (Defense)
* `Data Persistence` : مبدأ هندسي يعتمد على فصل دورة حياة الحاوية (التي يمكن حذفها وإعادة بنائها في أي وقت) عن دورة حياة البيانات (التي يجب أن تكون دائمة ومستقرة).
* `/home/sjoukni/data` : هو عبارة عن Bind Mount فرضه المشروع لضمان بقاء البيانات آمنة، وسهولة الوصول إليها من طرف المستخدم للنسخ الاحتياطي، ولحمايتها من أوامر تنظيف Docker.
* `/var/lib/docker` : هو المقر الرئيسي (Docker Root Directory) الذي يسيره محرك دوكر حصرياً. يُمنع التعديل عليه يدوياً لأنه يحتوي على حالة النظام الداخلية، الصور، والـ Managed Volumes الافتراضية.
* `Container Deletion` : عند مسح الحاوية (عبر docker rm أو docker compose down)، البيانات الموجودة في مسار /home/sjoukni/data لا تُحذف، لأنها مخزنة فعلياً في القرص الصلب للـ Host وليس داخل الحاوية.
* `docker compose down -v` : هذا الأمر يمسح فقط الـ Volumes الافتراضية التي يديرها Docker داخلياً، ولكنه لا يملك صلاحية مسح البيانات المربوطة يدوياً في مسارات الـ Host (مثل المجلد الخاص بك).

----

## 52. System Design: دمج Redis في docker-compose.yml
* `redis service` : إضافة خدمة جديدة ومستقلة خاصة بـ Redis لتعمل كخادم للتخزين المؤقت، التزاماً بقاعدة "حاوية لكل خدمة".
* `depends_on (wordpress)` : إضافة Redis إلى قائمة تبعيات WordPress. هذا يضمن أن نظام التشغيل سيعطي الأولوية لإقلاع خوادم البيانات (MariaDB و Redis) قبل تشغيل تطبيق الويب الذي يعتمد عليهما.
* `No Volume for Redis` : قرار معماري مدروس بعدم إضافة Volume لـ Redis، لأن الكاش يحتوي على بيانات متطايرة (Volatile) يتم إعادة توليدها تلقائياً بواسطة WordPress عند الحاجة، وبالتالي فإن التخزين الدائم ليس ضرورياً.
* `Internal Network` : عدم كشف منفذ Redis (6379) للمضيف (Host)، والاعتماد حصرياً على الاتصال الداخلي عبر شبكة inception_net لحماية الخادم من الوصول الخارجي المباشر.

---

## 55. System Design: هندسة خادم FTP
* [cite_start]`Shared Volumes` : الاعتماد على ربط نفس الـ Volume (وهو wordpress_data) بحاوية FTP. هذا يسمح بمشاركة البيانات بين الحاويات المعزولة، مما يمكن المستخدم من إدارة ملفات الموقع عن بعد[cite: 249].
* `Port 21 & Passive Ports` : فتح المنفذ 21 للتحكم، وفتح نطاق منافذ (21100-21110) لتفعيل نمط Passive Mode، وهو أمر ضروري لنقل البيانات بنجاح خلف جدار الحماية (NAT) الخاص بشبكة Docker.
* `chroot_local_user=YES` : قرار أمني في إعدادات vsftpd لسجن (Jail) مستخدم الـ FTP داخل مجلد /var/www/html فقط، لمنعه من تصفح باقي ملفات النظام داخل الحاوية.
* `exec vsftpd` : تشغيل خادم FTP كعملية رئيسية (PID 1) في الواجهة الأمامية للحفاظ على استمرارية تشغيل الحاوية.

----